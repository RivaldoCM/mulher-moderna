import styled from "styled-components";

export const Title = styled.div`
    width: 100%;
    padding: 3rem 0;

    h2{ color: var(--pink); }
    p{ color: var(--gray); }
`

export const Stats = styled.div`
    width: 100%;
    height: 18.25rem;
    background: var(--gray);
    border-radius: .5rem;
`