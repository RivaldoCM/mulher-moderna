import styled from "styled-components";

export const Container = styled.div`
    width: 100vw;
    flex-direction: row;

    main{
        width: calc(100vw - 23.5rem);
        height: 100vh;
        overflow: auto;
    }
`